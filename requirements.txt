# Graphifyy Portable Kit currently uses only the Python standard library.
# Keep this file so automated environments can run:
#   python -m pip install -r requirements.txt
# without installing third-party packages.
